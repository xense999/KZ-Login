```R
- 應用程式圖示換新，而且會跟著介面顏色走
| 舊的楓葉圖示換成新的兔子圖，做了兩個版本：介面用亮色就是奶油白底，用暗色就是深藍底。
| 不用去設定任何東西，設定頁一個新選項都沒有加，你照舊在裡面切亮色暗色就好。
| 切換之後不會馬上變，要"關掉程式再重新開啟"才會換過來。
| 以後更新版本也不會把它洗掉，下次開程式會自動變回你選的那一個。

- 會跟著換的位置
| 桌面捷徑、開始選單、釘在工作列上的那顆，還有程式開著時工作列顯示的圖示，四個地方一起換。
| 安裝資料夾裡執行檔本身的圖示固定是奶油白版，那一個換不了，不過平常不會看到。
| 萬一有哪一顆沒換成功，程式不會跳任何提示，只會在安裝資料夾裡留一張 icon.log 寫下是哪一顆。
```

請從下方 Assets 下載 `KuZe-Login_1.5.0_x64-setup.exe`。
