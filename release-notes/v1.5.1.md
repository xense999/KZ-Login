```R
- 修正：1.5.0 的新圖示對用「檢查更新」更新的人完全沒生效
| 1.5.0 把兩張圖示放在安裝檔裡，只有跑完整安裝程式才會送進去。
| 但程式裡的"檢查更新"是只換程式本體一個檔案的——這樣才不會把你釘在工作列上的捷徑拔掉。
| 結果就是：用檢查更新更新的人，圖示檔根本沒到手上，桌面和工作列的圖示一個都沒換。
| 這版把兩張圖直接做進程式本體裡，開啟時自己寫出來，用哪種方式更新都帶得到。

- 如果你已經裝了 1.5.0
| 直接用"檢查更新"更新到這一版就好，不必重新安裝。
| 更新完關掉程式再重新開啟一次，桌面和工作列的圖示就會換過來。
| 釘在工作列上的那顆如果還是舊的，那是 Windows 自己的圖示快取，重新登入一次就會刷新。
```

請從下方 Assets 下載 `KuZe-Login_1.5.1_x64-setup.exe`。
