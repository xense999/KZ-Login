```R
- 登入狀態判斷
| 修正在別的地方登入同一個帳號、這邊卻還顯示連線中的問題。
| 用手機、官方登入器、另一台電腦，或是在這裡對同一個帳號再掃一次碼，原本這邊的登入都會被伺服器擠掉。
| 以前認不出這種被擠掉的狀態，會一路顯示在線上，要等到按取得密碼失敗才發現。
| 現在改用伺服器自己的登入狀態檢查，被擠掉會馬上變灰點並跳出掃碼圖示。
| 伺服器如果哪天改了回應方式，這邊只會顯示"無法確認"等下次再問，不會把所有帳號都判成登出。

- 錯誤訊息
| 修正不管什麼錯誤都寫「登入已失效，請重新掃描」的問題。
| 網路連不上、或是沒安裝遊戲管理員時，會照實顯示原因，不會再叫你去掃碼。
```

請從下方 Assets 下載 `KuZe-Login_1.4.1_x64-setup.exe`。
