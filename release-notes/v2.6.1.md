```R
- 新增帳號 — 連結版本：在 LINE 裡貼上不會再斷掉
| 以前登入連結貼到 LINE 會從中間的"#"被切斷，後半段變成點不了的純文字，對方點開是壞的。
| 現在連結裡不再有"#"，長度也縮短了約三分之一，貼到 LINE 整條都點得到。以前發出去的舊連結照樣能開。
| 如果登入器拿到的登入連結格式不認得，會當場跳錯誤提示請你改用 QR Code，不會發出一條對方打不開的連結。

- 撤回 v2.6.0 的"一次只能開一個登入器"
| v2.6.0 短暫上架過，它會讓登入器只能開一個。這個限制已經拿掉，登入器跟以前一樣可以同時開好幾個。
```

請從下方 Assets 下載 `KuZe-Login_2.6.1_x64-setup.exe`。
