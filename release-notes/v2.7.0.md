```R
- 設定頁
| 新增"縮小到通知列"開關（在"系統設定"裡，預設關閉）：開啟後按縮小鈕會把登入器收進右下角通知列，點圖示叫回來，對圖示按右鍵可以結束。
| 關閉鈕不受這個開關影響，一律直接結束程式。
| "按鈕設定"搬進新的"系統設定"卡片；"系統設定"、"通知設定"、"遊戲路徑"平常只露標題，點標題才展開。

- 主視窗
| 滑鼠停在按鈕上的說明文字換成登入器自己的樣式，出現得比較快，也會跟著主題變色。
| 停用瀏覽器內建的快捷鍵（尋找、列印、下載、上一頁下一頁等），避免誤觸。複製、貼上、全選、復原不受影響。

- 程式圖標
| 取消暗色主題專用的深色圖標，一律使用亮色那一張。
| 原本用暗色主題的人，更新後開一次程式，桌面與工作列捷徑的圖標就會換回來。
```

請從下方 Assets 下載 `KuZe-Login_2.7.0_x64-setup.exe`。
