```R
- 設定：新增"按鈕設定"
| 可以決定主畫面右下角那顆按鈕要做什麼："登入"是原本的代理登入，"啟動"則是直接開遊戲。
| 切換後會立刻提示那顆按鈕變成什麼了。

- 主頁面：右下角按鈕可以改成"啟動遊戲"
| 遊戲沒開時按下去會直接開啟遊戲，開好後用各帳號右邊的箭頭把帳密打進去即可。
| 遊戲已經在跑時按下去會先詢問，確認後才強制關閉遊戲，效果等同工作管理員的"結束工作"。

- 主頁面：複製帳號與密碼時會顯示提示
| 按下"帳號"或"密碼"後，上方會顯示"已複製帳號"或"已複製密碼"，不會顯示複製到的內容。

- 主頁面：代理登入
| 修正剪貼簿是空的時候，跳出來的是一段英文訊息而不是"請先複製對方傳來的資料"的問題。

- 登入：減少跳出"我不是機器人"的次數
| 改用 Windows 內建的連線方式，並補上瀏覽器該有的識別資訊。原本的做法會被 beanfun 判定成自動化程式，每次帳密登入都要求通過驗證。
| 這是降低被要求驗證的機率，不保證完全不會跳。
```

請從下方 Assets 下載 `KuZe-Login_2.3.0_x64-setup.exe`。
