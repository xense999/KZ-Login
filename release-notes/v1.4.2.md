```R
- 登入狀態檢查
| 修正按 F5 一律顯示"無法確認登入狀態"的問題。
| 檢查登入狀態前要先跟伺服器要一組檢查碼，但已經登入的狀態下要不到，整個檢查就沒有結論。
| 改成用一條乾淨的連線去問，只帶著要檢查的那個帳號的登入憑證。
| 順便修正伺服器回"檢查碼不對"時會被當成已登出的問題，那種回覆是這邊自己問錯，不是你被登出，不會再因此把還在線上的帳號清掉。
| 真的無法確認時，訊息會寫出卡在哪一關，例如"取不到檢查碼"。

- 帳號瀏覽器
| 修正明明沒開任何瀏覽器，卻說"已有其他帳號的瀏覽器開著"的問題。
| Edge 在背景更新時會把開著的內建瀏覽器一起收掉，程式這邊卻還記得它開著，之後不管開哪個帳號都會被擋，要重開程式才會好。
| 改成開之前先確認那個視窗是不是真的還在，不在就直接放行。
```

請從下方 Assets 下載 `KuZe-Login_1.4.2_x64-setup.exe`。
