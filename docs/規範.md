# 規範 — 模組歸屬總表

> 此檔由 xen-skills 自動建立：模組歸屬總表——每個模組的 owner 程式與契約文件的索引。
> 純索引：契約內文寫在各模組的規範檔（`docs/規範/<模組名>.md`），這裡不放。引用模組契約一律指本表條目。

## 歸屬總表（每樣東西的唯一 owner）

| 模組 | owner 程式 | 規範檔 |
|------|-----------|--------|
| beanfun | `src-tauri/src/beanfun.rs` | |
| browser | `src-tauri/src/browser.rs` + `src-tauri/capabilities/browser.json` | [docs/規範/browser.md](規範/browser.md) |
| BrowserShell | `src/BrowserShell.vue` + `src/browser-main.ts` + `browser.html` | [docs/規範/browser.md](規範/browser.md) |
| win | `src-tauri/src/lib.rs` 的 `mod win` | |
| commands | `src-tauri/src/lib.rs`（`#[tauri::command]` 與 `AppState`） | |
| 主 UI | `src/App.vue` + `src/pages/` + `src/components/` | |
| 共用樣式 | `src/styles/main.css`（主題 token） | |
| accounts store | `src/stores/accounts.ts` | |

## 例外區（看似無主但刻意保留）

- `docs/ggm-data-decrypt.md` — GGM launch blob 的解密演算法研究紀錄。目前的登入流程刻意不解密（blob 原封轉交 GGM），所以沒有程式引用它；保留是因為 OTP 路線若復活會需要。
